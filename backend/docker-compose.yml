<<<<<<< HEAD
version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: bankofveyda-postgres
    environment:
      POSTGRES_DB: bankofveyda
      POSTGRES_USER: bankofveyda
      POSTGRES_PASSWORD: bankofveyda_dev
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U bankofveyda"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: bankofveyda-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  api:
    build: .
    container_name: bankofveyda-api
    command: python run.py
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: postgresql://bankofveyda:bankofveyda_dev@postgres:5432/bankofveyda
      REDIS_URL: redis://redis:6379/0
      DEBUG: "True"
      SECRET_KEY: "dev-secret-key-change-in-production"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    volumes:
      - .:/app
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 10s
      timeout: 5s
      retries: 5

  celery:
    build: .
    container_name: bankofveyda-celery
    command: celery -A app.workers.celery_app worker --loglevel=info --beat
    environment:
      DATABASE_URL: postgresql://bankofveyda:bankofveyda_dev@postgres:5432/bankofveyda
      REDIS_URL: redis://redis:6379/0
      DEBUG: "True"
      SECRET_KEY: "dev-secret-key-change-in-production"
    depends_on:
      - postgres
      - redis
    volumes:
      - .:/app

volumes:
  postgres_data:
  redis_data:
=======
version: '3.9'

services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: bankofveyda
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - '5432:5432'

  web:
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/app
    ports:
      - '8000:8000'
    environment:
      DJANGO_SECRET_KEY: replace-me-locally
      DJANGO_DEBUG: 'true'
      DJANGO_ALLOWED_HOSTS: 'localhost 127.0.0.1'
      POSTGRES_DB: bankofveyda
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_HOST: db
      POSTGRES_PORT: '5432'
    depends_on:
      - db

volumes:
  postgres_data:
>>>>>>> db7bb07 (commit)
